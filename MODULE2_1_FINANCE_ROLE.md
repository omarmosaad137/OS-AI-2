# Module 2.1 Finance Role

Run the migration:

supabase/migrations/module2_1_finance_role.sql

Then update app files and restart/deploy.

Finance user permissions:
- Sees all clients
- Sees all matters
- Sees all invoice requests
- Can approve/reject invoice requests
- Has Finance page
- Cannot create users
